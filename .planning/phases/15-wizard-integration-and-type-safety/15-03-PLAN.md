---
phase: 15-wizard-integration-and-type-safety
plan: 03
type: execute
wave: 3
depends_on:
  - "15-01"
  - "15-02"
files_modified:
  - scripts/smoke-phase15-wizard-integration.mjs
autonomous: true

must_haves:
  truths:
    - "A runnable smoke script exercises all four Phase 15 success criteria without network dependencies that the project does not already allow"
    - "The script asserts tsc --noEmit passes with zero errors (success criterion 4)"
    - "The script asserts SuggestedToolCall is exported as a literal union by inspecting src/audit/types.ts (success criterion 1)"
    - "The script asserts the switch-on-toolName is gone and a Record<SuggestedToolCall, FixHandler> dispatch table exists (success criterion 2)"
    - "The script asserts the gap-fill loop contains a pre-seed block referencing finding.suggestedToolCallArgs + recommendedType (success criterion 3)"
    - "The script exits non-zero when any assertion fails and prints [PASS]/[FAIL] per criterion"
  artifacts:
    - path: "scripts/smoke-phase15-wizard-integration.mjs"
      provides: "Offline regression gate for WIZ-01 + WIZ-02 + tsc-clean invariant"
      min_lines: 60
      contains: "success criterion"
  key_links:
    - from: "scripts/smoke-phase15-wizard-integration.mjs"
      to: "src/audit/types.ts"
      via: "readFile + regex assertion on SuggestedToolCall union"
      pattern: "SuggestedToolCall"
    - from: "scripts/smoke-phase15-wizard-integration.mjs"
      to: "src/tools/index.ts"
      via: "readFile + regex assertions on dispatch table + pre-seed block"
      pattern: "dispatchTable|recommendedType"
    - from: "scripts/smoke-phase15-wizard-integration.mjs"
      to: "npx tsc --noEmit"
      via: "spawn/execFile wrapping the TS compiler"
      pattern: "tsc.*--noEmit"
---

<objective>
Write `scripts/smoke-phase15-wizard-integration.mjs` — an offline regression gate that proves all four Phase 15 success criteria in a single `node scripts/smoke-phase15-wizard-integration.mjs` invocation. The script reads the source files that Plans 01 and 02 modified, applies regex assertions, and shells out to `npx tsc --noEmit` to prove the zero-errors invariant.

Purpose: Locks in the type-safety and pre-seed wins so any future refactor that regresses the dispatch table's exhaustiveness, reintroduces a switch, or drops the schema pre-seed is caught by the regression gate rather than discovered in production. Matches the pattern established by `smoke-phase12-framework.mjs`, `smoke-phase13-schema.mjs`, and `smoke-phase14-coverage.mjs`.
Output: An executable Node ESM script that prints one `[PASS]` or `[FAIL]` line per success criterion and exits with code 0 when all pass, non-zero otherwise.
</objective>

<execution_context>
@C:/Users/jackb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wizard-integration-and-type-safety/15-RESEARCH.md
@.planning/phases/15-wizard-integration-and-type-safety/15-01-SUMMARY.md
@.planning/phases/15-wizard-integration-and-type-safety/15-02-SUMMARY.md
@scripts/smoke-phase14-coverage.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author smoke-phase15-wizard-integration.mjs regression gate</name>
  <files>scripts/smoke-phase15-wizard-integration.mjs</files>
  <action>
Create `scripts/smoke-phase15-wizard-integration.mjs` as a Node ESM script (project is `"type": "module"`, so `.mjs` is not strictly required but keeps parity with sibling scripts). Before writing, read `scripts/smoke-phase14-coverage.mjs` to match its argv/exit/logging style exactly — same `[PASS]`/`[FAIL]` prefix conventions, same final summary line, same exit-1-on-any-failure behavior.

The script runs 4 independent checks, one per Phase 15 success criterion. Each check either reads a source file and applies regex assertions, or shells out to `npx tsc --noEmit`. No audit is run, no network is hit, no MCP server is started — this is a purely static regression gate (matches the offline character of `smoke-phase14-coverage.mjs`).

**Check 1 — Success criterion 1: SuggestedToolCall literal union exists.**

Read `src/audit/types.ts`. Assert:
- Matches regex `/export\s+type\s+SuggestedToolCall\s*=[\s\S]+?;/` — the exported type alias exists and terminates with a semicolon
- All 5 expected members appear verbatim inside the matched block: `'generate_llms_txt'`, `'configure_robots_txt'`, `'generate_schema_markup'`, `'generate_faq_content'`, `'generate_markdown_mirrors'`
- `AuditFinding.suggestedToolCall` is typed as `SuggestedToolCall` — grep for `/suggestedToolCall\?\s*:\s*SuggestedToolCall/` (NOT `string`)

Print `[PASS] SC-1: SuggestedToolCall literal union declared with all 5 members` on success. On failure, print `[FAIL] SC-1: <detail>` listing which sub-assertion failed.

**Check 2 — Success criterion 2: Typed dispatch table replaces switch.**

Read `src/tools/index.ts`. Assert:
- `grep -c "switch (toolName)"` returns 0 — the switch is gone
- Matches regex `/Record<SuggestedToolCall,\s*FixHandler>/` — the dispatch-table type literal is present
- Matches regex `/const\s+dispatchTable\s*:\s*Record<SuggestedToolCall/` — the table is declared as a typed const
- All 5 `SuggestedToolCall` members appear as property keys in the dispatch-table literal: use regex `/generate_llms_txt\s*:\s*async/`, `/configure_robots_txt\s*:\s*async/`, `/generate_schema_markup\s*:\s*async/`, `/generate_faq_content\s*:\s*async/`, `/generate_markdown_mirrors\s*:\s*async/`
- The dispatch call site appears — regex `/dispatchTable\[toolName\]\s*\(\s*finding\s*\)/` OR `/await\s+dispatchTable\[toolName\]/`
- `TOOL_FIELD_MAP` is also tightened — regex `/TOOL_FIELD_MAP[^:]*:\s*Record<SuggestedToolCall/`

Print `[PASS] SC-2: Typed Record<SuggestedToolCall, FixHandler> dispatch table with all 5 handlers; TOOL_FIELD_MAP also tightened` on success. On failure, list which regex missed.

**Check 3 — Success criterion 3: Pre-seed block seeds acc from suggestedToolCallArgs.**

Read `src/tools/index.ts`. Assert:
- Matches regex `/finding\.suggestedToolCallArgs/` — the pre-seed block reads the field
- Matches regex `/args\[['"]recommendedType['"]\]/` — specifically reads `recommendedType`
- Matches regex `/acc\.schemaTypes\s*=\s*\[/` — writes to `acc.schemaTypes` as an array
- The pre-seed block appears BEFORE the gap-fill elicitation call — validate by finding the index of the pre-seed regex match and the index of `server.server.elicitInput` inside the same `for (const finding of selectedFindings)` block. The pre-seed index MUST be lower than the elicit index. Use `String.indexOf` on the file contents after locating the gap-fill loop's `for` line (regex `/for\s*\(\s*const\s+finding\s+of\s+selectedFindings\s*\)/` — the first occurrence marks the gap-fill loop).
- Guard presence: matches regex `/!acc\.schemaTypes/` — the write is guarded to avoid overwriting user-supplied values

Print `[PASS] SC-3: acc pre-seeded from finding.suggestedToolCallArgs.recommendedType before gap-fill (guarded)` on success. On failure, list missing clauses.

**Check 4 — Success criterion 4: `tsc --noEmit` passes with zero errors.**

Use `node:child_process`'s `execFileSync` (synchronous — this script is sequential) to invoke `npx --no-install tsc --noEmit` from the project root. Capture stdout/stderr as strings.

Assert:
- Exit code 0 — `execFileSync` throws on non-zero; wrap in try/catch and report the thrown error's `stderr` as the failure message
- stdout + stderr combined is empty (or contains only the tsc noise expected on a clean compile — if tsc prints anything about "Found 0 errors", that is acceptable; anything containing `error TS` fails)

Use Windows-safe invocation: on Windows, `npx` may need to be invoked as `npx.cmd` via a shell. The sibling scripts use `execSync('npx tsc --noEmit', { stdio: 'inherit' })` or similar — mirror that exactly by reading one of `smoke-phase14-coverage.mjs`/`smoke-phase13-schema.mjs` to find the canonical invocation style in this project, then copy it.

Print `[PASS] SC-4: tsc --noEmit exits 0 (zero type errors)` on success. On failure, print `[FAIL] SC-4: tsc reported errors:` and the captured stderr.

**Script structure (follow smoke-phase14-coverage.mjs layout):**

```
#!/usr/bin/env node
// scripts/smoke-phase15-wizard-integration.mjs
// Offline regression gate for Phase 15: Wizard Integration and Type Safety.
// Asserts all four Phase 15 success criteria hold in the current source tree.

import { readFileSync } from 'node:fs';
import { execFileSync } from 'node:child_process';
import { fileURLToPath } from 'node:url';
import path from 'node:path';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const repoRoot = path.resolve(__dirname, '..');

const results = [];

function record(label, ok, detail = '') {
  results.push({ label, ok, detail });
  console.log(`${ok ? '[PASS]' : '[FAIL]'} ${label}${detail ? ': ' + detail : ''}`);
}

// --- Check 1: SuggestedToolCall union ---
// --- Check 2: Dispatch table ---
// --- Check 3: Pre-seed block ---
// --- Check 4: tsc --noEmit ---

const failures = results.filter(r => !r.ok);
console.log(`\nPhase 15 smoke: ${results.length - failures.length}/${results.length} checks passed.`);
process.exit(failures.length === 0 ? 0 : 1);
```

Make the script self-contained — no imports from `src/` or `dist/`, only Node stdlib. This is a static verification, not a runtime check.

Add a package.json note in the commit message (but do NOT modify package.json's scripts block — this phase doesn't add npm scripts; the script is invoked directly as `node scripts/smoke-phase15-wizard-integration.mjs`).

Do NOT modify any source file in this plan. This is a pure gate authoring task; if the gate fails on fresh execution, the failure indicates a bug in Plan 01 or Plan 02's implementation, not in this script.
  </action>
  <verify>
`cd "C:/Users/jackb/Documents/MyMCPServers/AI SEO Boost" && node scripts/smoke-phase15-wizard-integration.mjs` exits with code 0 and prints all 4 `[PASS]` lines.

Output includes `Phase 15 smoke: 4/4 checks passed.` as the final line.

Negative test (optional, revert after): temporarily rename `dispatchTable` to `switchTable` in `src/tools/index.ts`, re-run the script — SC-2 should print `[FAIL]` and the overall exit code should be 1. Restore the name.

Negative test (optional, revert after): temporarily remove the `!acc.schemaTypes` guard from `src/tools/index.ts`, re-run the script — SC-3 should print `[FAIL]`. Restore the guard.
  </verify>
  <done>
`scripts/smoke-phase15-wizard-integration.mjs` exists, runs, and prints `4/4 checks passed` with exit code 0; and the two negative-test modifications each force a specific `[FAIL]` line (confirming the script's regex detectors target the correct structural properties, not coincidental substrings).
  </done>
</task>

</tasks>

<verification>
Full phase-level checks:

1. `cd "C:/Users/jackb/Documents/MyMCPServers/AI SEO Boost" && node scripts/smoke-phase15-wizard-integration.mjs` — exit code 0, 4 `[PASS]` lines, zero `[FAIL]` lines
2. `cd "C:/Users/jackb/Documents/MyMCPServers/AI SEO Boost" && npx tsc --noEmit` — exit code 0 (confirms tsc invariant from Plan 01 + 02 holds; should be redundant with the smoke script's SC-4 check)
3. `cd "C:/Users/jackb/Documents/MyMCPServers/AI SEO Boost" && npm run build` — builds cleanly (end-to-end regression check)
</verification>

<success_criteria>
- `scripts/smoke-phase15-wizard-integration.mjs` exists and is executable via `node scripts/smoke-phase15-wizard-integration.mjs`
- Script exits 0 with all 4 `[PASS]` lines against the current tree (post-Plan-02)
- Script exits non-zero if any of the four Phase 15 success criteria regress
- Negative-test manipulations (rename `dispatchTable`, drop `!acc.schemaTypes` guard) each force a targeted `[FAIL]` — proving the regexes target structural intent, not incidental substrings
- No source files modified (only `scripts/` is touched)
</success_criteria>

<output>
After completion, create `.planning/phases/15-wizard-integration-and-type-safety/15-03-SUMMARY.md` documenting the four regex/command assertions, the ordering-check logic for SC-3 (pre-seed before elicit), and the confirmed 4/4 pass result — so future regressions surface immediately when the script is re-run.

After Plan 03 completes, Phase 15 is fully shipped:
- WIZ-01 (typed dispatch) and WIZ-02 (accumulator pre-seed) both landed
- All four success criteria verifiable in one command
- v1.2 milestone ready to close (Phases 11-15 complete)
</output>
