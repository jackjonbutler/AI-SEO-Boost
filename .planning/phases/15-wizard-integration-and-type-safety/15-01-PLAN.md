---
phase: 15-wizard-integration-and-type-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audit/types.ts
  - src/audit/dimensions/robots-txt.ts
autonomous: true

must_haves:
  truths:
    - "SuggestedToolCall union type exists in src/audit/types.ts with exactly 5 members matching the strings emitted by dimension files"
    - "AuditFinding.suggestedToolCall is typed as SuggestedToolCall (optional) rather than string (optional)"
    - "Robots-txt failing findings (missing bots, 404) now carry suggestedToolCallArgs with a missingBots array"
    - "tsc --noEmit exits with zero errors after the narrowing (all dimension files already emit exact-literal strings)"
  artifacts:
    - path: "src/audit/types.ts"
      provides: "SuggestedToolCall literal union + narrowed AuditFinding.suggestedToolCall"
      contains: "export type SuggestedToolCall"
    - path: "src/audit/dimensions/robots-txt.ts"
      provides: "Enriched findings carrying missingBots for WIZ-02 pre-seed / prompt enrichment"
      contains: "suggestedToolCallArgs"
  key_links:
    - from: "src/audit/types.ts"
      to: "all 5 dimension files"
      via: "AuditFinding.suggestedToolCall field type"
      pattern: "suggestedToolCall\\?: SuggestedToolCall"
    - from: "src/audit/dimensions/robots-txt.ts"
      to: "tools/index.ts gap-fill pre-seed (Plan 02)"
      via: "suggestedToolCallArgs.missingBots payload"
      pattern: "suggestedToolCallArgs:\\s*\\{\\s*missingBots"
---

<objective>
Declare the `SuggestedToolCall` string literal union in `src/audit/types.ts`, narrow `AuditFinding.suggestedToolCall` from `string` to `SuggestedToolCall`, and enrich robots-txt findings with `suggestedToolCallArgs: { missingBots }` so Plan 02 has a stable typed surface and a second audit-captured data producer to pre-seed from.

Purpose: Provides the type-level foundation (WIZ-01) and audit-side data enrichment (WIZ-02 producer) that Plan 02's dispatch-table refactor and accumulator pre-seeding consume. Landing types and dimension changes here in Wave 1 keeps Plan 02's file set limited to `tools/index.ts` alone.
Output: Narrowed `SuggestedToolCall` type exported from `src/audit/types.ts`, `AuditFinding.suggestedToolCall` typed as that union, and robots-txt findings emitting `suggestedToolCallArgs: { missingBots: string[] }` on all fail paths where `missing.length > 0`.
</objective>

<execution_context>
@C:/Users/jackb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wizard-integration-and-type-safety/15-RESEARCH.md
@src/audit/types.ts
@src/audit/dimensions/robots-txt.ts
@src/audit/dimensions/llms-txt.ts
@src/audit/dimensions/schema.ts
@src/audit/dimensions/faq.ts
@src/audit/dimensions/markdown.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare SuggestedToolCall union and narrow AuditFinding.suggestedToolCall</name>
  <files>src/audit/types.ts</files>
  <action>
Edit `src/audit/types.ts` to add a `SuggestedToolCall` literal-union type alias above the `AuditFinding` interface, then narrow `AuditFinding.suggestedToolCall` from `string` to `SuggestedToolCall`.

Exact members of the union (confirmed by grep across `src/audit/dimensions/*.ts` — RESEARCH.md Current State Audit table lists all 5):

```
'generate_llms_txt'
'configure_robots_txt'
'generate_schema_markup'
'generate_faq_content'
'generate_markdown_mirrors'
```

Insertion point: immediately above `export interface AuditFindingDiagnostics` (around line 17) or above `export interface AuditFinding` (around line 29). Keep it with the other audit-domain types. JSDoc the union so future maintainers know adding a value forces a dispatch-table compile error in `tools/index.ts`.

Required shape:
```ts
/**
 * Exhaustive union of all tool call strings the wizard can dispatch.
 * Every value here must have a corresponding key in TOOL_FIELD_MAP (tools/index.ts)
 * and a handler in the dispatch table. Adding a new tool name here forces a
 * compile error in the dispatch table until a handler is added.
 */
export type SuggestedToolCall =
  | 'generate_llms_txt'
  | 'configure_robots_txt'
  | 'generate_schema_markup'
  | 'generate_faq_content'
  | 'generate_markdown_mirrors';
```

Then change the existing `AuditFinding.suggestedToolCall` line (currently `suggestedToolCall?: string;` at line 34) to `suggestedToolCall?: SuggestedToolCall;`. Leave the existing `suggestedToolCallArgs?: Record<string, unknown>;` line (line 36) unchanged — it is already correctly declared from Phase 11.

Do NOT use a `const enum` or a runtime array (RESEARCH.md "Don't Hand-Roll" + Pitfall 1): plain string-literal union gives identical compile-time enforcement with zero runtime cost and is consistent with existing `Severity` and `AuditDimension` unions in this file.

Do NOT place the type in `src/types/index.ts` — per RESEARCH.md Pitfall 1, that module is documented as a leaf node with no imports, and `SuggestedToolCall` is audit-domain-scoped.

After the edit, run the type-checker to confirm every existing emission site still type-checks (all 5 dimension files already use exact string literals — RESEARCH.md table confirms 15 total emission sites, all on the union's member list).
  </action>
  <verify>
`cd "C:/Users/jackb/Documents/MyMCPServers/AI SEO Boost" && npx tsc --noEmit` exits with zero errors.

`grep -n "export type SuggestedToolCall" src/audit/types.ts` finds the new type.

`grep -n "suggestedToolCall?: SuggestedToolCall" src/audit/types.ts` finds the narrowed field.

`grep -rn "suggestedToolCall: '" src/audit/dimensions/` shows all 5 dimension files still compile — no TS errors flagged on any literal.
  </verify>
  <done>
`SuggestedToolCall` is exported from `src/audit/types.ts`, `AuditFinding.suggestedToolCall` is typed as `SuggestedToolCall | undefined`, `tsc --noEmit` is clean, and all 5 dimension files continue to compile without changes to their literal strings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed suggestedToolCallArgs.missingBots in robots-txt findings</name>
  <files>src/audit/dimensions/robots-txt.ts</files>
  <action>
Edit `src/audit/dimensions/robots-txt.ts` to add `suggestedToolCallArgs: { missingBots: missing }` to the two fail-path return sites where `missing.length > 0` (i.e. where some AI bots are absent from an otherwise-present robots.txt). Do NOT add `suggestedToolCallArgs` to the 404 fail paths — at that point no robots.txt exists so `missing` is not computed; the implicit meaning is "all bots missing" and the `configure_robots_txt` tool re-detects this from disk at execution time (RESEARCH.md Pitfall 6).

Fail sites to modify (confirmed by reading the file):

1. The URL path: the return at roughly line 74–81 (the branch inside `if (isUrl(target))` after `const missing = AI_BOTS.filter(...)`; status 'fail' with message `Missing AI crawler rules for: ${missing.join(', ')}`). Add `suggestedToolCallArgs: { missingBots: missing }` as a new property on that returned object.

2. The local-folder path: the return at roughly line 110–116 (after the local-folder `const missing = AI_BOTS.filter(...)`; status 'fail' with the same message pattern). Add the same `suggestedToolCallArgs: { missingBots: missing }` property.

Do NOT add `suggestedToolCallArgs` to:
- The 404 URL branch (line ~61–68) — no `missing` array computed there; the tool handler re-scans the on-disk file
- The local-folder ENOENT branch (line ~95–101) — same reason
- Any pass/warning returns — these have no `suggestedToolCall` and thus no args

`missing` is already a `string[]` (result of `AI_BOTS.filter(...)`), so `{ missingBots: missing }` is structurally compatible with `Record<string, unknown>`. No cast required.

Do NOT touch `dimensions/llms-txt.ts`, `dimensions/faq.ts`, or `dimensions/markdown.ts` in this task — per RESEARCH.md File-Level Change Summary, those dimensions have no audit-captured data worth seeding that maps to `AccumulatedContext`. `dimensions/schema.ts` already emits `suggestedToolCallArgs: { recommendedType: expectedType }` from Phase 13 — do NOT modify it.

Why enrich robots-txt now rather than leave for a future phase: it is the only remaining dimension with real audit-captured state (the `missing` bots list) that can meaningfully inform the gap-fill UX. Landing it here gives Plan 02 a second live data producer to exercise the pre-seed block against, proving the pattern is reusable.
  </action>
  <verify>
`cd "C:/Users/jackb/Documents/MyMCPServers/AI SEO Boost" && npx tsc --noEmit` exits with zero errors.

`grep -n "suggestedToolCallArgs" src/audit/dimensions/robots-txt.ts` shows exactly 2 occurrences (one URL branch, one local branch), both with the `{ missingBots: missing }` payload.

`grep -c "suggestedToolCall: 'configure_robots_txt'" src/audit/dimensions/robots-txt.ts` still returns 4 (count unchanged from RESEARCH.md audit — existing suggestedToolCall sites are preserved).
  </verify>
  <done>
Both `missing.length > 0` fail returns in `robots-txt.ts` emit `suggestedToolCallArgs: { missingBots: missing }`, `tsc --noEmit` is clean, and no other dimension files are touched.
  </done>
</task>

</tasks>

<verification>
Full phase-level checks after both tasks:

1. `cd "C:/Users/jackb/Documents/MyMCPServers/AI SEO Boost" && npx tsc --noEmit` — exit code 0, zero errors (Phase 15 success criterion 4 prerequisite)
2. `grep -rn "suggestedToolCall: '" src/audit/dimensions/` shows 15 emission sites total, every literal is a member of `SuggestedToolCall` (all 5 union members represented)
3. `grep -n "suggestedToolCall\?: SuggestedToolCall" src/audit/types.ts` finds exactly one match (the narrowed interface field)
4. No new files created — all edits in-place
</verification>

<success_criteria>
- `SuggestedToolCall` union is exported from `src/audit/types.ts` with exactly 5 members matching the inventory in RESEARCH.md
- `AuditFinding.suggestedToolCall` is typed as `SuggestedToolCall | undefined` (optional)
- `dimensions/robots-txt.ts` emits `suggestedToolCallArgs: { missingBots }` on both `missing.length > 0` fail paths
- `tsc --noEmit` passes with zero errors
- No modifications to `llms-txt.ts`, `schema.ts`, `faq.ts`, or `markdown.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/15-wizard-integration-and-type-safety/15-01-SUMMARY.md` describing the union members, the narrowed field, the robots-txt enrichment sites (line numbers), and the zero-error tsc confirmation — so Plan 02 can import `SuggestedToolCall` and consume the `missingBots` payload without re-inspecting these files.
</output>
