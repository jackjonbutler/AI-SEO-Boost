---
phase: 06-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: false

must_haves:
  truths:
    - "A developer running `npm install && npm run build` from a fresh clone produces `dist/index.js`"
    - "A developer can copy a claude_desktop_config.json snippet from the README and Claude Desktop lists all 8 tools"
    - "A developer using Claude Code can copy one `claude mcp add` command and Claude Code lists all 8 tools"
    - "Each of the 8 tools has a concrete example input and expected output in the README"
    - "The README labels `generate_location_service_pages` as a v2 stub in both the tool list and its own section"
    - "Known limitations (JS-rendered sites, UTF-8 only, page cap) appear in the README BEFORE the tool reference"
    - "Windows users see both the standard `%APPDATA%\\Claude\\claude_desktop_config.json` path and the MSIX path"
    - "Windows config snippet shows double-backslash path escaping (`C:\\\\Users\\\\...`)"
    - "README says `dist/` (not `build/`) everywhere a path appears"
    - "Troubleshooting section covers stdout corruption, missing `dist/`, and path-escaping failures"
  artifacts:
    - path: "README.md"
      provides: "Single-document install + config + tool reference + troubleshooting"
      min_lines: 300
      contains:
        - "ai-seo-boost"
        - "node dist/index.js"
        - "claude_desktop_config.json"
        - "claude mcp add"
        - "audit_ai_seo"
        - "generate_llms_txt"
        - "configure_robots_txt"
        - "generate_sitemap"
        - "generate_markdown_mirrors"
        - "generate_schema_markup"
        - "generate_faq_content"
        - "generate_location_service_pages"
        - "GPTBot"
        - "Known Limitations"
        - "Troubleshooting"
        - "v2 stub"
        - "Node"
  key_links:
    - from: "README Configuration section"
      to: "src/index.ts entry point"
      via: "node dist/index.js command in config snippet"
      pattern: "node .*dist[/\\\\]index\\.js"
    - from: "README Tools section"
      to: "src/tools/index.ts registered tool names"
      via: "exact tool name strings in each tool heading"
      pattern: "### (audit_ai_seo|generate_llms_txt|configure_robots_txt|generate_sitemap|generate_markdown_mirrors|generate_schema_markup|generate_faq_content|generate_location_service_pages)"
    - from: "README Tools section"
      to: "businessContextSchema in src/tools/index.ts"
      via: "shared parameter documentation block listing the 7 fields"
      pattern: "businessName.*businessType.*location.*services"
    - from: "README Installation section"
      to: "package.json scripts"
      via: "npm run build command matches tsc script"
      pattern: "npm (install|run build)"
---

<objective>
Write a single `README.md` at the repo root that lets any developer go from `git clone` to working Claude tools in under 10 minutes. This README is the entire Phase 6 deliverable: it contains prerequisites, installation, Claude Desktop and Claude Code configuration (with macOS/Windows/Windows-MSIX variants), a reference for all 8 tools with runnable examples, known limitations, and a troubleshooting section.

Purpose: Close DIST-01 — the final open requirement. The code is done; the README is what turns a source tree into a usable MCP server for anyone who clones it.

Output: `README.md` at repo root (~300-500 lines), committed to git.
</objective>

<execution_context>
@C:/Users/jackb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-distribution/06-RESEARCH.md

# Source-of-truth for exact tool names and input schemas — the README MUST match these
@src/tools/index.ts

# Confirms engines.node, scripts (build/start), module type, dependencies
@package.json

# Entry point used in the config snippet (node dist/index.js)
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README top half — header, requirements, installation, configuration, limitations</name>
  <files>README.md</files>
  <action>
Create README.md at the repo root with these sections, in this order:

1. **H1 + one-liner**
   - H1: `# AI SEO Boost`
   - One paragraph: "A TypeScript MCP server that generates llms.txt, markdown mirrors, XML sitemaps, robots.txt patches, JSON-LD schema markup, and FAQ content for any website. Point Claude Code or Claude Desktop at it and you get everything a site needs to be recommended by ChatGPT, Claude, and Perplexity — with zero manual file editing."
   - One line after: "Zero external API dependencies. No env vars required."

2. **## Requirements**
   - Bulleted list:
     - Node.js 18+ (Node 20 LTS or newer recommended — Node 18 reached EOL in April 2025)
     - git
     - Claude Desktop OR Claude Code

3. **## Installation** — numbered steps, exactly as in 06-RESEARCH.md "Example: Installation Sequence":
   ```bash
   # 1. Clone the repo
   git clone https://github.com/yourname/ai-seo-boost.git
   cd ai-seo-boost

   # 2. Install dependencies
   npm install

   # 3. Build the server
   npm run build

   # 4. Smoke-test (optional)
   node dist/index.js
   # Expected: process hangs waiting for stdin (server is running). Press Ctrl+C.
   ```
   Note after the block: "If `node dist/index.js` exits immediately with an error, the build failed. Re-run `npm run build` and check the TypeScript output."

4. **## Configuration** — three subsections:

   4a. **### Claude Desktop (macOS)**
   - File path: `~/Library/Application Support/Claude/claude_desktop_config.json`
   - JSON snippet using absolute path `/Users/yourname/ai-seo-boost/dist/index.js`:
     ```json
     {
       "mcpServers": {
         "ai-seo-boost": {
           "command": "node",
           "args": ["/Users/yourname/ai-seo-boost/dist/index.js"]
         }
       }
     }
     ```
   - After the JSON: "Replace `/Users/yourname/ai-seo-boost` with your repo's absolute path. Restart Claude Desktop."

   4b. **### Claude Desktop (Windows)**
   - Standard path: `%APPDATA%\Claude\claude_desktop_config.json`
   - MSIX (Microsoft Store) path: `%LOCALAPPDATA%\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\claude_desktop_config.json`
   - Tip: "If unsure which install you have, open Claude Desktop → Settings → Developer → Edit Config — it opens the correct file."
   - JSON snippet with double-backslash escaping:
     ```json
     {
       "mcpServers": {
         "ai-seo-boost": {
           "command": "node",
           "args": ["C:\\Users\\YourName\\ai-seo-boost\\dist\\index.js"]
         }
       }
     }
     ```
   - Critical warning block: "Windows paths in JSON require double-backslash escaping (`\\\\`). Single backslashes produce a JSON parse error and Claude Desktop will fail silently."

   4c. **### Claude Code**
   - Single command form:
     ```bash
     claude mcp add --transport stdio ai-seo-boost -- node /absolute/path/to/ai-seo-boost/dist/index.js
     ```
   - Verification: `claude mcp list` should show `ai-seo-boost`.
   - Note: "Do not commit `.mcp.json` to this repo — it hardcodes absolute paths that differ per machine."

5. **## Known Limitations** — bulleted, using the exact language from 06-RESEARCH.md Pattern 4:
   - **JavaScript-rendered sites:** The crawler fetches raw HTML only. React/Vue SPAs without SSR return empty or incomplete content — use a local folder target instead.
   - **Character encoding:** UTF-8 only. Non-UTF-8 pages may produce garbled content.
   - **Page cap:** The crawler has a per-run page limit. Very large sites are partially crawled — use a local folder for full-site processing.
   - **`generate_location_service_pages` is a v2 stub.** It accepts inputs but returns a not-implemented message. Do not use it in production workflows.

Do NOT write the Tools section or Troubleshooting in this task — those are Task 2 and Task 3.

Style notes:
- All JSON snippets must be valid JSON (verify by eye: matching braces, double-quoted keys).
- All bash snippets must work on copy-paste (no leading `$` prompt characters).
- Use `dist/` everywhere, never `build/`.
- Use the exact server name `ai-seo-boost` (matches package.json `name` field) in every config snippet.
  </action>
  <verify>
- `README.md` exists at repo root and has sections: H1, `## Requirements`, `## Installation`, `## Configuration` (with 3 subsections), `## Known Limitations`.
- Grep confirms presence of all keywords: `ai-seo-boost`, `node dist/index.js`, `claude_desktop_config.json`, `%APPDATA%\Claude`, `%LOCALAPPDATA%\Packages\Claude_pzs8sxrjxfjjc`, `claude mcp add`, `C:\\Users`, `v2 stub`.
- Grep for `build/` returns no matches (all paths use `dist/`).
- Grep for `npm install` and `npm run build` both return matches.
- `node -e "JSON.parse(require('fs').readFileSync('README.md','utf8').match(/\\{\\s*\"mcpServers\"[\\s\\S]*?\\n\\}/g)[0])"` — each fenced `mcpServers` JSON block parses as valid JSON.
  </verify>
  <done>
README.md exists with H1, Requirements, Installation (4 numbered steps), Configuration (macOS + Windows standard + Windows MSIX + Claude Code CLI), and Known Limitations sections. All JSON config snippets are syntactically valid. All paths use `dist/`. Server name is `ai-seo-boost` everywhere.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write README tool reference — shared businessContext block + 8 tool sections</name>
  <files>README.md</files>
  <action>
Append `## Tools` section after `## Known Limitations`. Structure:

1. **Short preface paragraph:** "This server exposes 8 tools. Seven are fully implemented; `generate_location_service_pages` is a v2 stub. Six tools accept a shared `businessContext` object — documented once below, then referenced per tool."

2. **### Shared parameter: `businessContext`**
   - Table of fields (use the exact fields from `src/tools/index.ts` `businessContextSchema` lines 29-37):

     | Field | Type | Required | Description |
     |-------|------|----------|-------------|
     | `businessName` | string | YES | The name of the business, exactly as it should appear in generated output |
     | `businessType` | string | YES | Type of business (e.g. "vehicle wrap shop", "law firm", "restaurant") |
     | `location` | string | no | Primary service area (e.g. "Denver, CO") |
     | `services` | string[] | no | List of services offered |
     | `website` | string | no | Canonical website URL (e.g. "https://example.com") |
     | `phoneNumber` | string | no | Contact phone number |
     | `description` | string | no | 1-3 sentence business description |

3. **Eight tool subsections**, one per tool, in this order (matching `src/tools/index.ts` registration order):

   Each subsection uses this exact template:
   ```
   ### {tool_name}

   **What it does:** One-sentence description.

   **Example input:**
   ```json
   { ... }
   ```

   **Expected output:** Brief prose describing return format, plus a truncated example if JSON output.
   ```

   Use the inputSchema for each tool (read from `src/tools/index.ts` — each `registerTool(name, {description, inputSchema})` call has the authoritative schema). Write realistic `businessContext` values (e.g. "Acme Wraps", "vehicle wrap shop", "Denver, CO").

   Per-tool content requirements:

   **audit_ai_seo** (line 55-86 in tools/index.ts): target (string, required), businessContext (optional). Input example: `target: "https://example.com"` with businessContext. Output: JSON report with `score`, `issues[]`, `suggestions[]`.

   **generate_llms_txt** (line 87-125): outputPath (string, required), businessContext (required). Example: `outputPath: "./llms.txt"`. Output: text confirming file written; the file contains H1 name, blockquote summary, and H2 Services/Locations/Contact sections.

   **configure_robots_txt** (line 126-170): targetPath (required), sitemapUrl (optional). Example: `targetPath: "./robots.txt"`, `sitemapUrl: "https://example.com/sitemap.xml"`. Output: text confirming allow-rules added for GPTBot, ClaudeBot, PerplexityBot, Google-Extended, CCBot plus Sitemap pointer.

   **generate_sitemap** (line 171-212): target (string, required — URL or folder), outputPath (required), businessContext (optional). Output: text confirming XML sitemap written with ISO 8601 lastmod and priority scores (1.0 home, 0.9 service, 0.8 info, 0.7 secondary).

   **generate_markdown_mirrors** (line 213-273): target (required), outputDir (required), businessContext (optional). Output: one `index.md` per page under outputDir, YAML frontmatter, chrome stripped.

   **generate_schema_markup** (line 274-313): businessContext (required), faqs (optional array of {question, answer}). Output: JSON-LD blocks (LocalBusiness, optionally FAQPage, Service array) returned as text — user pastes into HTML `<head>`. Truncate example JSON output with `"...": "..."` and note "Output truncated for readability."

   **generate_faq_content** (line 314-360): businessContext (required), count (optional, default 10, max 14). Output: JSON with `faqs` array of `{question, answer}` pairs. Show 2-3 example pairs then `...` with note "Output truncated — actual output contains 8-10 pairs." State: "Output is structured so it pipes directly into `generate_schema_markup` as the `faqs` parameter."

   **generate_location_service_pages** (line 361+): **Start the section with a bold stub-warning**: "**Status: v2 stub.** This tool is registered but returns a not-implemented message. It will accept and validate inputs but produce no output files." Then show the inputSchema for future reference. Example output: the stub text returned by `stubResponse(...)`.

Do NOT re-document `businessContext` fields per tool — reference the shared parameter block above.

Style notes:
- Every tool subsection must use the `###` heading with the exact tool name (grep-matchable).
- Every `**Example input:**` block must be a fenced ```json block with valid JSON.
- If a tool returns text content (not JSON), use a plain fenced block for `**Expected output:**`.
  </action>
  <verify>
- Grep `-n '^### '` README.md shows exactly 9 `###` headings: 1 for Shared parameter + 8 for tools.
- Grep for each tool name returns one match in a `###` heading: `audit_ai_seo`, `generate_llms_txt`, `configure_robots_txt`, `generate_sitemap`, `generate_markdown_mirrors`, `generate_schema_markup`, `generate_faq_content`, `generate_location_service_pages`.
- Grep for `Status: v2 stub` (or `v2 stub`) appears in the `generate_location_service_pages` section.
- Grep for all 7 businessContext fields (`businessName`, `businessType`, `location`, `services`, `website`, `phoneNumber`, `description`) returns matches in the shared-parameter table.
- Every `**Example input:**` block that follows is valid JSON (visual review — matching braces, quoted strings).
- No tool section invents parameters that don't exist in `src/tools/index.ts` — spot-check each tool's inputSchema against the example.
  </verify>
  <done>
Tools section exists with a preface, a shared `businessContext` parameter table listing all 7 fields with correct required/optional flags, and 8 tool subsections in registration order. Every tool has `### tool_name`, a what-it-does line, a JSON example input matching the tool's inputSchema, and an expected-output description. `generate_location_service_pages` is explicitly flagged as a v2 stub.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write README troubleshooting + final smoke-test verification</name>
  <files>README.md</files>
  <action>
Append `## Troubleshooting` section after the tool reference. Cover these four scenarios (from 06-RESEARCH.md Pitfalls):

### Tools don't appear in Claude Desktop
Possible causes in order of likelihood:
1. **`dist/index.js` does not exist.** Run `npm run build` in the repo directory. Confirm the file: `ls dist/index.js`.
2. **Path in config is relative or has a typo.** The `args` value must be an absolute path. On Windows, verify double-backslash escaping (`C:\\\\Users\\\\...`).
3. **Wrong config file edited (Windows).** If you installed Claude Desktop from the Microsoft Store, the config lives at `%LOCALAPPDATA%\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\claude_desktop_config.json`, not `%APPDATA%\Claude`.
4. **Claude Desktop was not restarted.** Quit fully (not just close the window) and reopen.

### Tools don't appear in Claude Code
1. Run `claude mcp list` — if `ai-seo-boost` isn't listed, re-run the `claude mcp add` command with an absolute path.
2. Restart any running Claude Code session after adding.

### All tools fail simultaneously after a code change
If every tool returns an error after editing source and rebuilding, the most common cause is **stdout corruption**: a `console.log()` somewhere in the codebase writes to stdout and breaks the JSON-RPC stream. This server uses stderr (`console.error`) exclusively. Grep the codebase for `console.log` and remove or convert any hits.

### JSON parse error loading config
Windows-specific: single backslashes in paths are JSON escape characters and produce invalid JSON. Use `\\\\` (double backslash) in every path within `args`.

After the Troubleshooting section, add a small `## License` section: "MIT" (or match existing license decision — if none, use "MIT" as a sensible default for open-source distribution).

**Final smoke-test verification — run these commands in order and confirm each passes:**

1. `npm install` — installs dependencies without error.
2. `npm run build` — produces `dist/index.js`.
3. `ls dist/index.js` — file exists.
4. `node dist/index.js` — process hangs (server running). Kill with Ctrl+C after 2 seconds.
5. Open README.md and visually confirm: every `###` tool section has a JSON example input, the Windows path uses double-backslashes, `dist/` (not `build/`) everywhere, `generate_location_service_pages` explicitly labeled as v2 stub.

If the smoke test reveals any issue (build fails, missing section, broken JSON), fix it before marking this task done. Do NOT ship a broken README.
  </action>
  <verify>
- Grep README.md for `## Troubleshooting` returns one match.
- Grep for the four troubleshooting subsections: `Tools don't appear in Claude Desktop`, `Tools don't appear in Claude Code`, `All tools fail simultaneously`, `JSON parse error`.
- Grep for `console.log` (as documentation of the anti-pattern) appears in the Troubleshooting section.
- `npm run build` exits 0 and `dist/index.js` exists.
- `timeout 3 node dist/index.js` exits with a timeout code (not an error exit) — proves the server actually runs.
- `wc -l README.md` >= 300 lines.
- Grep `-c '^### '` README.md returns >= 9 (shared param + 8 tools; troubleshooting subsections use `###` too, so the count will exceed 9 — that's fine).
  </verify>
  <done>
README.md has a Troubleshooting section covering the four scenarios (missing tools in Desktop, missing tools in Code, stdout corruption, Windows JSON escape), plus a License section. `npm run build` succeeds, `dist/index.js` is produced, and `node dist/index.js` starts without crashing. README is at least 300 lines and visually complete.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — dogfood the README from a fresh clone perspective</name>
  <what-built>
A complete `README.md` at the repo root documenting: prerequisites, installation, Claude Desktop (macOS + Windows standard + Windows MSIX) + Claude Code configuration, all 8 tools with runnable example inputs and expected outputs, known limitations, and troubleshooting.
  </what-built>
  <how-to-verify>
This README is the public face of the project. A human needs to read it as if they were a first-time user cloning the repo. Claude cannot simulate "I don't already know this codebase."

Open `README.md` in a Markdown preview (VS Code: Ctrl+Shift+V) and verify:

1. **Readability test (5 min):** Read the README top-to-bottom without looking at any source code. At the end, you should feel you could (a) install the server, (b) configure it for your Claude client, (c) call any of the 8 tools. If any step feels unclear, note it.

2. **Config snippet copy-paste test:** Copy the macOS config snippet into a text editor. Replace the placeholder path with a real absolute path. Save as JSON. Does your editor flag any syntax errors? (It shouldn't.)

3. **Windows path test:** Look at the Windows JSON snippet. Confirm every backslash in `args` is doubled (`\\`). If you see a single `\`, the snippet is broken.

4. **Tool example test:** Pick 2 tools at random (e.g. `generate_llms_txt` and `generate_faq_content`). For each:
   - Does the `Example input` JSON parse cleanly? (Paste into https://jsonlint.com or a JSON validator.)
   - Does the parameter list match what you'd expect from a tool of that name?

5. **Stub tool visibility test:** Find `generate_location_service_pages` in the README. Is it obvious this tool is a v2 stub both (a) in the tool list/preface and (b) in its own section? If you missed either, the warning isn't loud enough.

6. **Limitations visibility test:** Can a reader notice "JS-rendered sites aren't supported" BEFORE they attempt to call `generate_markdown_mirrors` on a React site? The limitations section must appear before the tool reference.

7. **End-to-end smoke test (optional but recommended):** In a fresh terminal:
   - `git status` (confirm README.md is the only/main unstaged change)
   - `npm run build` (confirm clean build)
   - `node dist/index.js` (confirm server starts — press Ctrl+C)
   - Edit your actual `claude_desktop_config.json` or run the `claude mcp add` command per the README. Restart Claude Desktop or Claude Code. Confirm all 8 tools appear.

If steps 1-6 pass, approve. If step 7 reveals any config/instruction gap (e.g. you follow the README literally and tools don't appear), describe the gap so Task 3 (or a new task) can close it.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe any issues found (e.g. "Windows snippet has single backslash on line X" or "generate_sitemap example is missing outputPath").</resume-signal>
</task>

</tasks>

<verification>
Phase-level checks after all tasks complete:

1. `README.md` exists at `c:/Users/jackb/Documents/MyMCPServers/AI SEO Boost/README.md`.
2. `wc -l README.md` >= 300 lines.
3. All 8 tool names appear exactly once each as `### ` headings (grep count == 8 for the set).
4. All 4 config paths documented: macOS (`~/Library/Application Support/Claude/`), Windows standard (`%APPDATA%\Claude\`), Windows MSIX (`%LOCALAPPDATA%\Packages\Claude_`), Claude Code (`claude mcp add`).
5. All 5 AI bots mentioned in configure_robots_txt section: GPTBot, ClaudeBot, PerplexityBot, Google-Extended, CCBot.
6. No occurrence of `build/` as a path (only `dist/`).
7. `npm run build` succeeds with no TypeScript errors.
8. `node dist/index.js` starts the server (hangs on stdin — valid stdio server behavior).
9. Every JSON config snippet parses as valid JSON (visual/tool inspection).
10. `generate_location_service_pages` labeled as v2 stub in at least 2 places (preface and own section).
11. Human verification (Task 4) approved.
</verification>

<success_criteria>
Phase 6 closes DIST-01 when:

- [ ] README.md exists at repo root, committed to git.
- [ ] Roadmap success criterion 1: Contains a working `claude_desktop_config.json` snippet pointing to `node dist/index.js` — verified by valid JSON and absolute path placeholder.
- [ ] Roadmap success criterion 2: Each of 8 tools has a documented example input and expected output — verified by 8 `###` tool sections with `**Example input:**` and `**Expected output:**` blocks.
- [ ] Roadmap success criterion 3: Known limitations (JS-rendered sites, UTF-8 only, page cap) are documented — verified by `## Known Limitations` section.
- [ ] Roadmap success criterion 4: A developer with Node 18+ can run `npm install && npm run build` and have a working server — verified by the smoke test in Task 3 and human dogfood in Task 4.
- [ ] Human verification (Task 4) approved.
- [ ] All must_haves truths confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution/06-01-SUMMARY.md` documenting:
- Final README.md line count and section count
- Any deviations from the plan (e.g. added sections, reordered content)
- Human verification outcome (what the reviewer flagged, if anything)
- Confirmation that DIST-01 is closed and Phase 6 is complete
- Closing note: the project ships — roadmap is 6/6 phases complete
</output>
