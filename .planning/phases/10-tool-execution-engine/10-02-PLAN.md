---
phase: 10-tool-execution-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - scripts/smoke-audit-wizard-fork.mjs
autonomous: true

must_haves:
  truths:
    - "Scenario A asserts 'Wizard complete' instead of the Phase 9 marker string"
    - "Scenario J exercises the full Phase 10 path and passes SMOKE OK"
    - "Scenario J asserts the final response is NOT valid JSON (it is plain text)"
    - "All 10 scenarios pass with SMOKE OK when the smoke test is run"
    - "Scenario J's synthesized gap-fill responses provide /tmp/ paths for file-writing tools"
  artifacts:
    - path: "scripts/smoke-audit-wizard-fork.mjs"
      provides: "10 scenarios covering complete wizard pipeline"
      contains: "scenarioJ"
  key_links:
    - from: "scenarioA"
      to: "Wizard complete assertion"
      via: "text.includes('Wizard complete')"
      pattern: "Wizard complete"
    - from: "scenarioJ"
      to: "synthesizeGapFillResponse"
      via: "accepts gap-fill calls with /tmp paths"
      pattern: "scenarioJ"
---

<objective>
Update the smoke test to reflect Phase 10's changed return shape and add Scenario J — a full end-to-end test of the execution engine. Two tasks: update Scenario A's assertion (Phase 9 marker → "Wizard complete"), then add Scenario J that walks the wizard through acceptance, gap-fill, tool execution, and confirms a plain-text session summary is returned.

Purpose: Prove EXEC-01 through EXEC-05 work end-to-end with a reproducible, automated smoke test.
Output: `scripts/smoke-audit-wizard-fork.mjs` extended to 10 scenarios, all passing.
</objective>

<execution_context>
@C:/Users/jackb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-context-accumulation/09-01-SUMMARY.md
@.planning/phases/10-tool-execution-engine/10-RESEARCH.md
@.planning/phases/10-tool-execution-engine/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Scenario A and add Scenario J to smoke test</name>
  <files>scripts/smoke-audit-wizard-fork.mjs</files>
  <action>
    Read the full smoke test file before editing (it is ~600 lines). Make two changes:

    **Change 1 — Update Scenario A:**
    Scenario A currently asserts:
    ```javascript
    text.includes('Context accumulation complete — tool execution lands in Phase 10')
    ```
    Replace this assertion (and its surrounding assert() call) with:
    ```javascript
    assert(
      text.includes('Wizard complete'),
      `Scenario A: response missing 'Wizard complete' summary. Got: ${text.slice(0, 300)}`,
    );
    assert(
      !text.startsWith('{'),
      `Scenario A: response should be plain text, not JSON. Got: ${text.slice(0, 100)}`,
    );
    ```
    Also update the scenario label string to remove the "(Phase 9 envelope)" reference — change to "(Phase 10 — full wizard pipeline, plain-text summary)".

    Also check lines ~391, ~454, ~545 where Scenarios G, H, I may assert `text.includes('Context accumulation complete')` — update each of those assertions the same way (replace with `text.includes('Wizard complete')`). Read the file carefully first to find every instance.

    **Change 2 — Add Scenario J:**
    Add `async function scenarioJ()` after the last scenario function (scenarioI, line ~503). Scenario J must:

    1. Label: `'Scenario J (Phase 10 — end-to-end execution, plain-text session summary)'`

    2. Set up a mock server that intercepts all elicitInput calls:
       - Call 1: wizard/report fork → choose 'wizard'
       - Call 2: issue selection multi-select → accept all (return all dimension:status keys as selected)
       - Calls 3+: gap-fill elicitations → use `synthesizeGapFillResponse()` helper (already exists in the file). For any gap-fill field that looks like a path (field name contains 'Path' or 'Dir'), return a valid `/tmp/smoke-wizard-j/` prefixed path. For `outputPath`, return `/tmp/smoke-wizard-j/llms.txt`. For `robotsPath`, return `/tmp/smoke-wizard-j/robots.txt`. For `outputDir`, return `/tmp/smoke-wizard-j/mirrors/`.
       - Per-tool confirmation calls (elicitInput with 'Fix applied:' in message): respond with `{ acknowledged: 'yes' }`.

    3. How to distinguish call types in the handler:
       - If `message.includes('Fix applied:')` → it is a per-tool confirmation → respond `{ acknowledged: 'yes' }`
       - If `requestedSchema.properties` has `mode` field → it is the wizard/report fork → respond `{ mode: 'wizard' }`
       - If `requestedSchema.properties` has dimension:status-style keys (multi-select checklist) → it is issue selection → accept all (return an object with all keys set to their const values)
       - Otherwise → it is a gap-fill → use synthesizeGapFillResponse with path overrides for Path/Dir fields

    4. Run the `audit_ai_seo` tool call with a real local `target` (use the project root, already used in other scenarios: same pattern as Scenario A)

    5. Assertions after the call:
       ```javascript
       assert(typeof text === 'string', label + ': response must be a string');
       assert(text.includes('Wizard complete'), label + ': response must contain session summary');
       assert(!text.startsWith('{'), label + ': response must be plain text, not JSON');
       ```

    6. Add `await scenarioJ();` to the `main()` function after `await scenarioI();`

    7. Update the comment block at the top of the file (the multi-line scenario list comment, lines ~1-15) to add:
       `*   Scenario J — Phase 10 full execution: wizard → accept-all → gap-fills → tool execution → session summary`

    Run the smoke test after editing to confirm all 10 scenarios pass:
    ```
    node scripts/smoke-audit-wizard-fork.mjs
    ```
    Fix any assertion failures before marking done.
  </action>
  <verify>
    Run: `node scripts/smoke-audit-wizard-fork.mjs`
    Expected output: 10 lines of `SMOKE OK: Scenario A (...)` through `SMOKE OK: Scenario J (...)`. No SMOKE FAIL lines. Exit code 0.

    Also verify:
    - `grep -c "Wizard complete" scripts/smoke-audit-wizard-fork.mjs` returns at least 4 (Scenarios A, G, H, I, J all assert it)
    - `grep -c "Context accumulation complete" scripts/smoke-audit-wizard-fork.mjs` returns 0 (Phase 9 marker fully removed from assertions)
    - `grep -n "scenarioJ" scripts/smoke-audit-wizard-fork.mjs` shows the function definition and the call in main()
  </verify>
  <done>
    `node scripts/smoke-audit-wizard-fork.mjs` exits 0 with 10 SMOKE OK lines. Scenario A no longer asserts the Phase 9 marker. Scenario J asserts "Wizard complete" and plain-text (non-JSON) response. No SMOKE FAIL appears.
  </done>
</task>

</tasks>

<verification>
- `node scripts/smoke-audit-wizard-fork.mjs` → 10 SMOKE OK lines, exit 0
- `grep "SMOKE FAIL" scripts/smoke-audit-wizard-fork.mjs` finds zero matches in output (not in file assertions)
- `grep -c "Wizard complete" scripts/smoke-audit-wizard-fork.mjs` ≥ 4
- `grep -c "Context accumulation complete" scripts/smoke-audit-wizard-fork.mjs` = 0
</verification>

<success_criteria>
1. All 10 smoke scenarios pass with SMOKE OK
2. Scenario A label updated to reflect Phase 10 (no "Phase 9 envelope")
3. Scenario J added: exercises full wizard path including per-tool confirmation responses
4. Zero remaining references to the Phase 9 marker string in smoke test assertions
5. `npm run build` still passes (no changes to .ts files in this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/10-tool-execution-engine/10-02-SUMMARY.md`
</output>
