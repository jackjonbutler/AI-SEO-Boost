---
phase: 10-tool-execution-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/index.ts
autonomous: true

must_haves:
  truths:
    - "Each selected issue is dispatched to exactly one generator function — no issue silently falls through"
    - "Tools execute in the order selectedFindings arrives (severity-sorted from Phase 8)"
    - "patchRobotsTxt is called without a subsequent writeFile — it handles its own I/O"
    - "generate_faq_content and generate_schema_markup results appear as text in fixResults (no file write attempted)"
    - "generate_markdown_mirrors re-crawls target and writes one file per page"
    - "A per-tool elicitInput confirmation fires after each success, wrapped in try/catch (non-blocking)"
    - "The function returns exactly once — a plain-text session summary after the loop completes"
    - "A tool error pushes to fixErrors and the loop continues to the next finding"
    - "npm run build passes with zero TypeScript errors"
  artifacts:
    - path: "src/tools/index.ts"
      provides: "Phase 10 execution loop replacing Phase 9 final return"
      contains: "Wizard complete"
  key_links:
    - from: "selectedFindings loop"
      to: "switch on finding.suggestedToolCall"
      via: "for...of + switch"
      pattern: "for.*selectedFindings"
    - from: "generate_markdown_mirrors case"
      to: "acquireLocal / crawlUrl re-crawl"
      via: "target from outer closure"
      pattern: "acquireLocal|crawlUrl"
    - from: "configure_robots_txt case"
      to: "patchRobotsTxt return value"
      via: "botsAdded + sitemapAdded"
      pattern: "patchRobotsTxt"
---

<objective>
Replace the Phase 9 final `return` in `src/tools/index.ts` with the Phase 10 sequential execution loop. The loop iterates `selectedFindings`, dispatches each to the correct generator via a `switch` on `finding.suggestedToolCall`, writes output to disk, elicits a per-tool confirmation (non-blocking), accumulates results, and returns a single plain-text session summary.

Purpose: Deliver EXEC-01 through EXEC-05 — the tool execution engine that resolves selected issues sequentially without user re-intervention.
Output: Modified `src/tools/index.ts` with Phase 10 loop replacing the Phase 9 marker return. Build passes.
</objective>

<execution_context>
@C:/Users/jackb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-context-accumulation/09-01-SUMMARY.md
@.planning/phases/10-tool-execution-engine/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Phase 9 final return with Phase 10 execution loop</name>
  <files>src/tools/index.ts</files>
  <action>
    Locate the Phase 9 final return block inside the `if (useWizard)` branch (lines ~364-376), which currently returns a JSON envelope with `marker: '[wizard] Context accumulation complete — tool execution lands in Phase 10'`. Replace it entirely with the Phase 10 execution loop and session summary return.

    Implementation — replace the Phase 9 return with this structure:

    **Step 1: Initialize accumulators (immediately after Phase 9 contextSummary is built)**
    ```
    const fixResults: string[] = [];
    const fixErrors: string[] = [];
    ```

    **Step 2: Sequential execution loop**
    ```
    for (const finding of selectedFindings) {
      if (skippedFindings.includes(finding.dimension)) continue;
      const toolName = finding.suggestedToolCall;
      if (!toolName || !TOOL_FIELD_MAP[toolName]) continue;

      switch (toolName) {
        case 'generate_llms_txt': { ... break; }
        case 'configure_robots_txt': { ... break; }
        case 'generate_schema_markup': { ... break; }
        case 'generate_faq_content': { ... break; }
        case 'generate_markdown_mirrors': { ... break; }
      }
    }
    ```

    **Switch cases — implement each:**

    `generate_llms_txt`:
    - Wrap in try/catch, push to fixErrors on throw
    - `const ctx = acc as BusinessContext;`
    - `const content = buildLlmsTxt(ctx);`
    - `await writeFile(acc.outputPath!, content, 'utf-8');`
    - `fixResults.push('llms.txt written to ' + acc.outputPath + ' (' + content.length + ' bytes)');`

    `configure_robots_txt`:
    - Wrap in try/catch, push to fixErrors on throw
    - `const result = await patchRobotsTxt(acc.robotsPath!, acc.sitemapUrl);`
    - Build parts array: if botsAdded.length > 0, push "Added N bot allow-rule(s): ..."; else push "All AI bot allow-rules already present"
    - If result.sitemapAdded, push "Added Sitemap: " + acc.sitemapUrl
    - `fixResults.push('robots.txt — ' + parts.join('; '));`
    - Do NOT call writeFile — patchRobotsTxt handles its own I/O

    `generate_schema_markup`:
    - Wrap in try/catch, push to fixErrors on throw
    - `const ctx = acc as BusinessContext;`
    - `const blocks = buildSchemaMarkup(ctx, acc.schemaTypes as SchemaType[]);`
    - No file write — schema markup returned as text for user to copy
    - `fixResults.push('schema markup generated (' + blocks.length + ' block(s) — copy JSON-LD into <head>):\n' + blocks.join('\n\n'));`

    `generate_faq_content`:
    - Wrap in try/catch, push to fixErrors on throw
    - `const ctx = acc as BusinessContext;`
    - `const pairs = buildFaqContent(ctx);`
    - No file write — FAQ pairs returned as text for user to use
    - `fixResults.push('FAQ content generated (' + pairs.length + ' pairs):\n' + JSON.stringify(pairs, null, 2));`

    `generate_markdown_mirrors`:
    - Wrap in try/catch, push to fixErrors on throw
    - Re-crawl target (not in Phase 9 envelope — must re-acquire):
      ```
      const t = target.trim();
      const isUrl = t.startsWith('http://') || t.startsWith('https://');
      const results = isUrl ? await crawlUrl(t) : await acquireLocal(t);
      const docs: MarkdownDocument[] = results.filter((r): r is MarkdownDocument => !isAcquisitionError(r));
      ```
    - If docs.length === 0: push error to fixErrors and break
    - Use same slug collision handling and pLimit(5) pattern as the standalone `generate_markdown_mirrors` handler already in this file (copy the writtenSlugs Set + disambiguate function + limit pattern verbatim)
    - `fixResults.push(written.length + ' markdown mirror(s) written under ' + acc.outputDir!);`

    **Step 3: Per-tool confirmation (wrap each switch case's success path AFTER the switch block is done for that finding)**

    Actually, place the per-tool confirmation INSIDE each switch case, after fixResults.push, before break:
    ```
    try {
      await server.server.elicitInput({
        mode: 'form',
        message: 'Fix applied: ' + fixResults[fixResults.length - 1],
        requestedSchema: {
          type: 'object',
          properties: {
            acknowledged: { type: 'string', title: 'Continue', oneOf: [{ const: 'yes', title: 'OK' }] },
          },
          required: ['acknowledged'],
        },
      });
    } catch {
      // Non-elicitation client — silently continue
    }
    ```

    **Step 4: Session summary return (replaces Phase 9 return entirely)**
    ```
    const summaryLines: string[] = [
      'Wizard complete. ' + fixResults.length + ' fix(es) applied.',
    ];
    if (fixResults.length > 0) {
      summaryLines.push('', 'Applied:');
      summaryLines.push(...fixResults.map((r) => '  - ' + r));
    }
    if (fixErrors.length > 0) {
      summaryLines.push('', 'Errors:');
      summaryLines.push(...fixErrors.map((e) => '  - ' + e));
    }
    if (skippedFindings.length > 0) {
      summaryLines.push('', 'Skipped (gap-fill cancelled):');
      summaryLines.push(...skippedFindings.map((d) => '  - ' + d));
    }
    return {
      content: [{ type: 'text' as const, text: summaryLines.join('\n') }],
    };
    ```

    Type cast notes:
    - Use `acc as BusinessContext` inside each switch case (safe — Phase 9 guarantees required fields are present before calling generator)
    - Use `acc.schemaTypes as SchemaType[]` for buildSchemaMarkup (Phase 9 post-processes schemaTypes to always be string[]; constrained by gap-fill schema to valid SchemaType values)
    - Both casts follow the precedent of `as any` cast used in Phase 9 (see 09-01-SUMMARY.md)

    Run `npm run build` after editing. Fix any TypeScript errors before proceeding.
  </action>
  <verify>
    Run: `npm run build`
    Expected: Zero TypeScript errors. No "TS2345", no "TS2339", no "TS7006" errors.

    Visual check: The Phase 9 `return { content: [{ type: 'text' as const, text: JSON.stringify({ marker: '[wizard] Context accumulation complete...` block is gone. The function now returns `summaryLines.join('\n')` once at the bottom of the `if (useWizard)` branch.
  </verify>
  <done>
    `npm run build` exits with code 0. The if(useWizard) branch ends with a single return containing "Wizard complete." text, not a JSON envelope with a marker string. Each of the 5 generator functions is dispatched from a switch case with try/catch. patchRobotsTxt has no subsequent writeFile call.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `grep -n "Wizard complete" src/tools/index.ts` returns one match
- `grep -n "Context accumulation complete" src/tools/index.ts` returns zero matches (Phase 9 marker removed)
- `grep -n "writeFile.*robotsPath" src/tools/index.ts` returns zero matches (patchRobotsTxt not double-written)
- `grep -c "fixResults.push" src/tools/index.ts` returns 5 (one per tool case)
</verification>

<success_criteria>
1. `npm run build` exits 0 — no TypeScript errors
2. The Phase 9 marker string no longer exists in index.ts
3. Each of the 5 switch cases (generate_llms_txt, configure_robots_txt, generate_schema_markup, generate_faq_content, generate_markdown_mirrors) is present and wrapped in try/catch
4. Session summary return (single return, plain text, "Wizard complete.") exists at end of if(useWizard) branch
5. Per-tool elicitInput calls are wrapped in try/catch inside each case
</success_criteria>

<output>
After completion, create `.planning/phases/10-tool-execution-engine/10-01-SUMMARY.md`
</output>
