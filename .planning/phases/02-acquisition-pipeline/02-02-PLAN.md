---
phase: 02-acquisition-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/acquisition/crawl.ts
autonomous: true

must_haves:
  truths:
    - "crawlUrl(seedUrl, opts) fetches pages starting from seedUrl and returns AcquisitionResult[]"
    - "The crawler stays on the same domain — it never follows links to external hostnames"
    - "No more than opts.pageCap pages are returned, even if more same-domain links exist"
    - "Each fetch uses its own AbortSignal.timeout(opts.timeoutMs) so a slow page doesn't cancel others"
    - "A page that returns non-2xx status or throws produces an AcquisitionError entry, not a crash"
    - "The same URL is never fetched twice — deduplication happens at enqueue time via a visited Set"
    - "All href values in returned MarkdownDocument.markdown are absolute"
  artifacts:
    - path: "src/acquisition/crawl.ts"
      provides: "crawlUrl(seedUrl, opts) — BFS crawl with p-limit concurrency"
      exports: ["crawlUrl", "CrawlOptions"]
      contains: "pLimit"
  key_links:
    - from: "src/acquisition/crawl.ts"
      to: "src/processing/strip.ts"
      via: "import { stripChrome }"
      pattern: "stripChrome"
    - from: "src/acquisition/crawl.ts"
      to: "src/processing/convert.ts"
      via: "import { convertToMarkdown }"
      pattern: "convertToMarkdown"
    - from: "src/acquisition/crawl.ts"
      to: "p-limit"
      via: "import pLimit from 'p-limit'"
      pattern: "pLimit\\("
    - from: "crawlUrl loop"
      to: "visited Set"
      via: "visited.add() at enqueue time"
      pattern: "visited\\.add"
---

<objective>
Build the URL crawl acquisition module: a BFS crawler that fetches pages concurrently (via p-limit), stays on the seed domain, respects a hard page cap and per-request timeout, and returns AcquisitionResult[].

Purpose: Completes the acquisition pipeline so all tools can accept either a local folder path OR a live URL. The crawl branch is the live-URL counterpart to acquireLocal().
Output: src/acquisition/crawl.ts exporting crawlUrl() and CrawlOptions.
</objective>

<execution_context>
@C:/Users/jackb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-acquisition-pipeline/02-RESEARCH.md
@.planning/phases/02-acquisition-pipeline/02-01-SUMMARY.md
@src/types/index.ts
@src/processing/strip.ts
@src/processing/convert.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build crawl acquisition — crawl.ts</name>
  <files>src/acquisition/crawl.ts</files>
  <action>
    Create src/acquisition/crawl.ts with the following implementation. Read strip.ts and convert.ts exports first to confirm exact function signatures before importing.

    --- src/acquisition/crawl.ts ---

    // src/acquisition/crawl.ts
    // BFS URL crawler with p-limit concurrency, AbortSignal.timeout() per-request,
    // hard page cap, same-domain filtering, and error-safe page processing.
    //
    // Design decisions (documented per RESEARCH.md):
    // - p-limit@6 (NOT 7) — Node 18 compat; identical API
    // - AbortSignal.timeout(ms) per fetch — NOT a shared controller; each request independent
    // - visited Set updated at enqueue time — prevents duplicate fetches in concurrent batches
    // - Never use cheerio.fromURL() — bypasses p-limit and AbortSignal entirely
    // - All errors produce AcquisitionError entries — never throw to caller
    // Defaults: pageCap=50, concurrency=3, timeoutMs=10000

    import pLimit from 'p-limit';
    import * as cheerio from 'cheerio';
    import { stripChrome } from '../processing/strip.js';
    import { convertToMarkdown } from '../processing/convert.js';
    import type { AcquisitionResult, MarkdownDocument, AcquisitionError } from '../types/index.js';

    export interface CrawlOptions {
      /** Hard maximum number of pages to fetch and return. Default: 50. */
      pageCap: number;
      /** Maximum simultaneous HTTP requests. Default: 3. */
      concurrency: number;
      /** Per-request timeout in milliseconds. Default: 10000. */
      timeoutMs: number;
    }

    /** Default CrawlOptions values — MCP tool parameters override these. */
    export const DEFAULT_CRAWL_OPTIONS: CrawlOptions = {
      pageCap: 50,
      concurrency: 3,
      timeoutMs: 10_000,
    };

    export async function crawlUrl(
      seedUrl: string,
      opts: CrawlOptions = DEFAULT_CRAWL_OPTIONS,
    ): Promise<AcquisitionResult[]> {
      const base = new URL(seedUrl);
      const baseDomain = base.hostname;

      // visited tracks URLs at ENQUEUE time — prevents duplicates across concurrent batches
      const visited = new Set<string>([seedUrl]);
      const queue: string[] = [seedUrl];
      const results: AcquisitionResult[] = [];
      const limit = pLimit(opts.concurrency);

      while (queue.length > 0 && results.length < opts.pageCap) {
        // Take up to `concurrency` URLs from the front of the queue
        const batchSize = Math.min(opts.concurrency, queue.length, opts.pageCap - results.length);
        const batch = queue.splice(0, batchSize);

        const batchResults = await Promise.all(
          batch.map(url => limit(() => fetchPage(url, baseDomain, opts.timeoutMs))),
        );

        for (const item of batchResults) {
          if (results.length >= opts.pageCap) break;
          results.push(item.result);

          // Enqueue new same-domain links discovered on this page
          for (const link of item.discoveredLinks) {
            if (!visited.has(link) && results.length + queue.length < opts.pageCap) {
              visited.add(link);  // Add at enqueue time — not at fetch time
              queue.push(link);
            }
          }
        }
      }

      return results;
    }

    // ---------------------------------------------------------------------------
    // Internal helpers — not exported; tools import from acquisition/ only
    // ---------------------------------------------------------------------------

    interface FetchPageResult {
      result: AcquisitionResult;
      discoveredLinks: string[];
    }

    async function fetchPage(
      url: string,
      baseDomain: string,
      timeoutMs: number,
    ): Promise<FetchPageResult> {
      try {
        // AbortSignal.timeout() — each call is independent; a timeout on one fetch
        // does not affect other concurrent fetches.
        const res = await fetch(url, { signal: AbortSignal.timeout(timeoutMs) });

        if (!res.ok) {
          const error: AcquisitionError = { url, error: `HTTP ${res.status}`, source: 'crawl' };
          return { result: error, discoveredLinks: [] };
        }

        const raw = await res.text();

        // Discover links from raw HTML BEFORE stripping — chrome elements (nav, header)
        // often contain the most internal links.
        const discoveredLinks = extractSameDomainLinks(raw, url, baseDomain);

        const { html, title, description } = stripChrome(raw, url);
        const markdown = convertToMarkdown(html);

        const doc: MarkdownDocument = {
          url,
          title,
          description,
          markdown,
          frontmatter: { title, url, description },
          source: 'crawl',
        };

        return { result: doc, discoveredLinks };
      } catch (err) {
        const errorMsg = err instanceof Error ? err.message : String(err);
        const error: AcquisitionError = { url, error: errorMsg, source: 'crawl' };
        return { result: error, discoveredLinks: [] };
      }
    }

    function extractSameDomainLinks(html: string, pageUrl: string, baseDomain: string): string[] {
      const $ = cheerio.load(html);
      const links: string[] = [];

      $('a[href]').each((_, el) => {
        const href = $(el).attr('href');
        if (!href) return;

        try {
          // new URL(href, pageUrl) handles: protocol-relative (//), root-relative (/path),
          // relative (../page), query-only (?q=1), fragment-only (#x) — all correctly
          const abs = new URL(href, pageUrl);

          // Same-domain check: compare hostname (NOT string prefix — prefix is unsafe)
          // e.g. 'example.com.evil.com' would pass a prefix check but fails hostname check
          if (
            abs.hostname === baseDomain &&
            (abs.protocol === 'http:' || abs.protocol === 'https:')
          ) {
            // Normalise: strip fragment to avoid fetching same page twice with different anchors
            abs.hash = '';
            links.push(abs.href);
          }
        } catch {
          // Malformed href — skip silently
        }
      });

      // Deduplicate within this page's link list before returning to caller
      return [...new Set(links)];
    }

    Key constraints (enforcement, not just documentation):
    - NEVER use cheerio.fromURL() — bypasses p-limit and AbortSignal. Use cheerio.load(htmlString).
    - NEVER create a single shared AbortController for all pages — each fetch() call needs its own AbortSignal.timeout(timeoutMs).
    - NEVER add a URL to the queue without first adding it to visited — add to visited at enqueue time.
    - All imports use .js extensions (Node16 ESM).
    - The function signature default parameter `opts = DEFAULT_CRAWL_OPTIONS` means callers can pass a partial-override if needed; tools should pass explicit opts from their input schema.
  </action>
  <verify>
    npm run build (npx tsc) succeeds with no errors.
    npx tsc --noEmit passes.

    Integration smoke test (requires a live URL):
    node --input-type=module &lt;&lt;'EOF'
    import { crawlUrl, DEFAULT_CRAWL_OPTIONS } from './dist/acquisition/crawl.js';
    const results = await crawlUrl('https://example.com', { ...DEFAULT_CRAWL_OPTIONS, pageCap: 2 });
    console.error('count:', results.length);
    console.error('first:', JSON.stringify(results[0], null, 2));
    EOF

    Verify:
    - results.length is 1 or 2 (example.com has few internal links)
    - results[0].source === 'crawl'
    - results[0].url === 'https://example.com/'
    - results[0].markdown is non-empty
    - No uncaught exceptions

    Also verify the no-network error case (bad URL):
    node --input-type=module &lt;&lt;'EOF'
    import { crawlUrl, DEFAULT_CRAWL_OPTIONS } from './dist/acquisition/crawl.js';
    const results = await crawlUrl('https://this-domain-does-not-exist-12345.invalid', { ...DEFAULT_CRAWL_OPTIONS, pageCap: 1 });
    console.error('error entry:', JSON.stringify(results[0]));
    EOF
    Verify: results[0].error is a non-empty string, results[0].source === 'crawl', no throw.
  </verify>
  <done>
    crawlUrl('https://example.com', { pageCap: 2, concurrency: 3, timeoutMs: 10000 }) returns 1-2 MarkdownDocument entries with populated title, markdown, and url. An unreachable URL returns an AcquisitionError entry without throwing. TypeScript compilation is clean. The same URL does not appear twice in results.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify full acquisition pipeline end-to-end</name>
  <files></files>
  <action>
    This task performs integration verification of the complete Phase 2 pipeline. No new files are created.

    Run the following checks in order:

    1. Full compile check:
       npx tsc --noEmit
       Expected: zero errors

    2. Build:
       npm run build
       Expected: exits 0

    3. Existing server still starts:
       Run: node dist/index.js and immediately send Ctrl+C (or use timeout 2 node dist/index.js).
       Expected: process starts without uncaught exception (connection on stdio — no crash)

    4. Local acquisition end-to-end (two pages, one with a nav, one with a relative link):
       Create temp files:
         mkdir -p /tmp/phase2-test
         Write /tmp/phase2-test/page1.html:
           <html><head><title>Page One</title></head><body>
           <nav><a href="/page2">Nav link</a></nav>
           <main><h1>Page One</h1><a href="/page2">Relative link</a></main>
           </body></html>
         Write /tmp/phase2-test/page2.html:
           <html><head><title>Page Two</title><meta name="description" content="Second page"></head>
           <body><main><p>Content here</p></main></body></html>

       Then run:
       node --input-type=module &lt;&lt;'EOF'
       import { acquireLocal } from './dist/acquisition/local.js';
       import { isAcquisitionError } from './dist/types/index.js';
       const results = await acquireLocal('/tmp/phase2-test');
       const docs = results.filter(r => !isAcquisitionError(r));
       const errors = results.filter(r => isAcquisitionError(r));
       console.error('total:', results.length, 'docs:', docs.length, 'errors:', errors.length);
       for (const doc of docs) {
         console.error('---');
         console.error('title:', doc.title);
         console.error('url:', doc.url);
         console.error('has absolute links:', !doc.markdown.includes('href="/'));
       }
       EOF

       Expected:
       - total: 2, docs: 2, errors: 0
       - Both docs have non-empty title
       - Both docs have url starting with file://
       - No relative hrefs (/page2 style) in markdown output (they should be absolute file:// or removed)

    5. Crawl acquisition end-to-end:
       node --input-type=module &lt;&lt;'EOF'
       import { crawlUrl } from './dist/acquisition/crawl.js';
       import { isAcquisitionError } from './dist/types/index.js';
       const results = await crawlUrl('https://example.com', { pageCap: 3, concurrency: 2, timeoutMs: 10000 });
       const docs = results.filter(r => !isAcquisitionError(r));
       console.error('results:', results.length, 'docs:', docs.length);
       if (docs[0]) {
         console.error('first url:', docs[0].url);
         console.error('first title:', docs[0].title);
         console.error('source:', docs[0].source);
       }
       EOF

       Expected:
       - results >= 1
       - docs[0].source === 'crawl'
       - docs[0].url is an absolute https:// URL
       - No throw, no uncaught promise rejection

    If any check fails, diagnose and fix before marking this task done. Do not skip checks.
  </action>
  <verify>All 5 checks above pass. npx tsc --noEmit exits 0. Both local and crawl smoke tests produce the expected output as described.</verify>
  <done>
    Phase 2 success criteria verified:
    1. Local pipeline returns one MarkdownDocument per .html file with YAML frontmatter and chrome stripped.
    2. Crawl pipeline respects pageCap and returns MarkdownDocuments from a live URL.
    3. All hrefs in returned markdown are absolute (no /relative or ../paths).
    4. Failed pages return AcquisitionError entries, not exceptions.
  </done>
</task>

</tasks>

<verification>
Phase 2 complete when all four ROADMAP success criteria are verifiable:
1. Local: acquireLocal('/tmp/phase2-test') returns 2 docs with title, markdown, file:// url
2. Crawl: crawlUrl('https://example.com', { pageCap: 3, ... }) returns 1-3 docs, respects cap
3. Absolute URLs: no relative hrefs in any returned doc.markdown
4. Error safety: unreachable URL to crawlUrl returns AcquisitionError entry, no throw

TypeScript:
- npx tsc --noEmit passes
- npm run build succeeds

Existing server:
- node dist/index.js still starts on stdio without error (Phase 1 not broken)
</verification>

<success_criteria>
- crawl.ts created and exports crawlUrl() and CrawlOptions and DEFAULT_CRAWL_OPTIONS
- crawlUrl('https://example.com', { pageCap: 3, concurrency: 2, timeoutMs: 10000 }) returns 1-3 MarkdownDocument entries
- An unreachable seed URL returns AcquisitionError entry without throwing
- Zero TypeScript errors across all src/ files
- All four Phase 2 ROADMAP success criteria are demonstrably met
</success_criteria>

<output>
After completion, create `.planning/phases/02-acquisition-pipeline/02-02-SUMMARY.md` using the summary template.
</output>
