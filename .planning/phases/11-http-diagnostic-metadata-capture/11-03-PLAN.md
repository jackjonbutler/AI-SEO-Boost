---
phase: 11-http-diagnostic-metadata-capture
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - src/audit/index.ts
autonomous: true

must_haves:
  truths:
    - "AuditReport returned by runAudit() includes pagesAudited array when any finding has diagnostics"
    - "pagesAudited lists the exact URLs that dimension checks probed (checkedUrl values)"
    - "pagesAudited is undefined (not empty array) when no finding has diagnostics"
    - "tsc --noEmit passes with zero errors across the entire codebase"
    - "Sort order of findings is unchanged (severity-first)"
  artifacts:
    - path: "src/audit/index.ts"
      provides: "runAudit() populates pagesAudited on AuditReport"
      contains: "pagesAudited"
  key_links:
    - from: "src/audit/index.ts"
      to: "src/audit/types.ts"
      via: "AuditReport.pagesAudited field assignment"
      pattern: "pagesAudited"
---

<objective>
Populate `AuditReport.pagesAudited` in `runAudit()` by collecting `checkedUrl` values from finding diagnostics after `Promise.all` resolves — completing DIAG-03.

Purpose: Callers of `audit_ai_seo` can now see the exact URLs the audit probed, satisfying the requirement that crawl scope is verifiable without re-running.
Output: One modified file; final `tsc --noEmit` clean pass confirming all Phase 11 changes integrate without errors.
</objective>

<execution_context>
@C:/Users/jackb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/audit/index.ts
@.planning/phases/11-http-diagnostic-metadata-capture/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate pagesAudited in runAudit()</name>
  <files>src/audit/index.ts</files>
  <action>
    After `Promise.all` resolves and before the sort in `runAudit()`, derive `pagesAudited` from findings' diagnostics blocks, then include it in the returned `AuditReport`.

    1. After `const findings = await Promise.all([...])`, add:
       ```typescript
       // Collect URLs actually probed by dimension checks (DIAG-03).
       // Only dimensions wired with diagnostics (llms-txt, robots-ai) contribute.
       // If no finding has diagnostics, pagesAudited is undefined — not an empty array.
       const probedUrls = findings
         .map(f => f.diagnostics?.checkedUrl)
         .filter((u): u is string => u !== undefined);
       const pagesAudited = probedUrls.length > 0 ? probedUrls : undefined;
       ```

    2. Update the return statement to include `pagesAudited`:
       ```typescript
       return {
         target: trimmed,
         generatedAt: new Date().toISOString(),
         findings,
         pagesAudited,
       };
       ```

    3. The sort (`findings.sort(...)`) remains between the probedUrls derivation and the return. Sort order is unchanged.

    No new imports needed — `AuditReport` type import already covers the extended interface from Plan 01.

    Important: `pagesAudited` is `undefined` (not `[]`) when no findings have diagnostics — this happens for local-path audits and for findings from dimensions not yet wired (schema.ts, faq.ts, markdown.ts). The type is `string[] | undefined`, matching `AuditReport.pagesAudited?: string[]`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>tsc passes zero errors; runAudit() return type includes pagesAudited; pagesAudited is string[] with one entry per finding.diagnostics.checkedUrl, or undefined when no diagnostics exist.</done>
</task>

<task type="auto">
  <name>Task 2: Full codebase type-check and smoke verification</name>
  <files></files>
  <action>
    Run a complete TypeScript compilation check across the entire project to confirm all Phase 11 changes integrate without errors. Then verify the key structural invariants introduced in this phase by inspecting the modified files.

    1. Run: `npx tsc --noEmit`
       Expected: zero errors, zero warnings.

    2. If any errors appear, address them before proceeding. Common causes:
       - `responseTimeMs` used before assignment (TypeScript definite assignment analysis) — fix by initializing `let responseTimeMs = 0;`
       - `contentLengthHeader` used outside its scope — ensure it's declared in the outer scope if used after the try/catch
       - Missing `diagnostics` type import in dimensions files

    3. Check that `src/tools/index.ts` (wizard dispatch) still compiles — it reads `AuditFinding` fields by name and the new optional fields must not break its type-narrowing logic.

    4. Run `npm run build` to confirm the compiled output is also clean (tsc --noEmit catches type errors but not module resolution issues that only appear at emit time).
  </action>
  <verify>npx tsc --noEmit exits 0 AND npm run build exits 0</verify>
  <done>Both tsc --noEmit and npm run build complete with zero errors. The dist/ output is up to date.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`: zero errors
- `npm run build`: zero errors, dist/index.js updated
- Manual spot-check: src/audit/index.ts return statement includes `pagesAudited`
- Manual spot-check: pagesAudited derivation uses `.filter((u): u is string => u !== undefined)` type guard (not `!== null` — checkedUrl is string, not string | null)
</verification>

<success_criteria>
- runAudit() return object includes pagesAudited field
- pagesAudited is undefined for local-path audits (no diagnostics in those findings)
- pagesAudited is string[] for URL audits with at least one wired dimension (llms-txt or robots-ai)
- tsc --noEmit: zero errors across all modified files (audit/types.ts, types/index.ts, acquisition/crawl.ts, audit/dimensions/llms-txt.ts, audit/dimensions/robots-txt.ts, audit/index.ts)
- npm run build: zero errors
- No changes to any tool handler, wizard dispatch, or other dimension checks required
</success_criteria>

<output>
After completion, create `.planning/phases/11-http-diagnostic-metadata-capture/11-03-SUMMARY.md`
</output>
