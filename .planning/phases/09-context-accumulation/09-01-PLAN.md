---
phase: 09-context-accumulation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/index.ts
  - scripts/smoke-audit-wizard-fork.mjs
autonomous: true

must_haves:
  truths:
    - "When businessContext is provided at wizard start, no field from it is asked again during any tool invocation (CTX-01)"
    - "When no upfront context is provided, the wizard asks only for the fields required by the first tool that needs them (CTX-02)"
    - "Any field answered mid-wizard is carried forward — subsequent tools never prompt for it again (CTX-03)"
    - "The accumulated context state is visible/traceable via a contextSummary string in the Phase 9 return envelope (success criterion 4)"
    - "User cancel on a gap-fill elicitation skips only that tool, not the entire wizard; skippedFindings are reported in the envelope (Pitfall 5)"
    - "Findings without a suggestedToolCall or with an unknown tool name are silently skipped in the loop (Pitfall 4)"
    - "Phase 9 return envelope includes marker, selectedFindings, skippedFindings, accumulatedContext, contextSummary — matching the Phase 10 input contract"
  artifacts:
    - path: "src/tools/index.ts"
      provides: "TOOL_FIELD_MAP, AccumulatedContext + WizardToolFields types, context accumulator loop replacing Phase 8 return"
      contains: "TOOL_FIELD_MAP"
    - path: "src/tools/index.ts"
      provides: "Phase 9 marker string in the return envelope"
      contains: "Context accumulation complete"
    - path: "scripts/smoke-audit-wizard-fork.mjs"
      provides: "Three new scenarios proving CTX-01, CTX-02, CTX-03"
      contains: "Scenario G"
  key_links:
    - from: "src/tools/index.ts accumulator loop"
      to: "TOOL_FIELD_MAP"
      via: "TOOL_FIELD_MAP[finding.suggestedToolCall] lookup per finding"
      pattern: "TOOL_FIELD_MAP\\[.*suggestedToolCall"
    - from: "src/tools/index.ts accumulator loop"
      to: "server.server.elicitInput"
      via: "gap-fill elicitation per tool with missing required fields only"
      pattern: "server\\.server\\.elicitInput"
    - from: "src/tools/index.ts accumulator merge"
      to: "Object.assign on acc"
      via: "merges elicitInput result.content into acc after each gap-fill"
      pattern: "Object\\.assign\\(acc"
    - from: "scripts/smoke-audit-wizard-fork.mjs Scenario G"
      to: "src/tools/index.ts Phase 9 marker"
      via: "assert text includes 'Context accumulation complete'"
      pattern: "Context accumulation complete"
---

<objective>
Replace the Phase 8 terminal `return` in the `if (useWizard)` branch of `audit_ai_seo` with a context accumulator loop that reuses upfront `businessContext`, gathers only missing required fields per tool, carries answers forward, and returns a Phase 10-ready envelope.

Purpose: Satisfies CTX-01 (no re-asking), CTX-02 (lazy field gather only when needed), CTX-03 (accumulate answers across findings), and success criterion 4 (traceable state). Unblocks Phase 10 tool execution.

Output: `src/tools/index.ts` with `TOOL_FIELD_MAP` constant, `AccumulatedContext` type, and accumulator loop; `scripts/smoke-audit-wizard-fork.mjs` extended with three new scenarios (G, H, I) exercising the context accumulation contract.
</objective>

<execution_context>
@C:/Users/jackb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-context-accumulation/09-RESEARCH.md
@.planning/phases/08-issue-selection/08-01-SUMMARY.md

@src/tools/index.ts
@src/types/index.ts
@scripts/smoke-audit-wizard-fork.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TOOL_FIELD_MAP + types and replace Phase 8 return with context accumulator loop</name>
  <files>src/tools/index.ts</files>
  <action>
Edit `src/tools/index.ts` in two surgical locations. Do not touch any other tool registration, import, or the `businessContextSchema`.

**Location A — module scope, immediately after the `businessContextSchema` definition (after line 37, before `stubResponse`):**

Add three module-scope declarations exactly as specified in RESEARCH.md:

1. `type WizardToolFields = { outputPath?: string; robotsPath?: string; sitemapUrl?: string; schemaTypes?: string[]; outputDir?: string; }` — covers tool-specific fields not in `BusinessContext`.
2. `type AccumulatedContext = Partial<BusinessContext> & WizardToolFields` — the accumulator's type.
3. `const TOOL_FIELD_MAP: Record<string, { contextRequired: (keyof BusinessContext)[]; contextOptional: (keyof BusinessContext)[]; toolRequired: (keyof WizardToolFields)[]; toolOptional: (keyof WizardToolFields)[]; }> = { ... }` — populate ALL FIVE entries exactly as in RESEARCH.md §Code Examples "TOOL_FIELD_MAP declaration" (generate_llms_txt, configure_robots_txt, generate_schema_markup, generate_faq_content, generate_markdown_mirrors). Use `as const` assertions where the Record needs them to satisfy `keyof BusinessContext`.

**Location B — the `audit_ai_seo` handler, the current `return { content: [...JSON.stringify({marker: '[wizard] Issue selection complete ...', selectedFindings, businessContext: businessContext ?? null})] }` block (currently lines 179-188):**

Replace that single `return` statement with the full accumulator loop from RESEARCH.md §Code Examples "Full Phase 9 accumulator loop (replaces final return in if(useWizard) branch)". Preserve every surrounding line (try/catch boundary, the empty-selection guard, the selectedKeys filter). The replacement block must:

1. Seed `const acc: AccumulatedContext = { ...businessContext ?? {} }` and `const skippedFindings: string[] = []`.
2. Loop `for (const finding of selectedFindings)`. Inside the loop:
   - Guard: `const toolName = finding.suggestedToolCall; if (!toolName || !TOOL_FIELD_MAP[toolName]) continue;` (Pitfall 4).
   - Compute `missingContextRequired` by filtering `fieldSpec.contextRequired` against `acc[f] === undefined`.
   - Compute `missingToolRequired` by filtering `fieldSpec.toolRequired` against `(acc as Record<string, unknown>)[f] === undefined`.
   - If `allMissing.length === 0`, `continue` — satisfies CTX-01/CTX-03 (do not re-elicit).
   - Build `properties: Record<string, unknown>` and `required: string[]` via a `switch (field)` covering every key that can appear in `allMissing`: `businessName`, `businessType`, `location`, `services` (string — will be split post-merge), `website`, `phoneNumber`, `description`, `outputPath`, `robotsPath`, `sitemapUrl` (optional — `required.pop()` after push), `schemaTypes` (array/multi-select with the three anyOf options `LocalBusiness` / `FAQPage` / `Service` and `default: ['LocalBusiness']`), `outputDir`. Titles and descriptions exactly as in RESEARCH.md.
   - Call `await server.server.elicitInput({ mode: 'form', message: \`To fix "${finding.dimension}" (${finding.severity}), I need a few more details:\`, requestedSchema: { type: 'object', properties: properties as Record<string, ReturnType<typeof String>>, required } })`. No try/catch — per Phase 8 precedent, errors propagate to outer catch (RESEARCH Pitfall decision carry-forward).
   - If `gapResult.action !== 'accept'`, push `finding.dimension` to `skippedFindings` and `continue` (Pitfall 5 — per-tool skip, not global abort).
   - Merge: `Object.assign(acc, gapResult.content)`.
   - Post-process `services`: `if (typeof acc.services === 'string') { acc.services = (acc.services as string).split(',').map(s => s.trim()).filter(Boolean); }` (Pitfall 2).
   - Post-process `schemaTypes`: `if (acc.schemaTypes !== undefined && !Array.isArray(acc.schemaTypes)) { acc.schemaTypes = [String(acc.schemaTypes)]; }` (Pitfall 3).
3. After the loop, build `contextSummary` by iterating `Object.entries(acc)`, filtering undefined values, formatting as `- key: value` lines (`Array.isArray` → `join(', ')`, else `String(v)`). If no entries, fall back to `'No context gathered (all tools operate without business details)'`.
4. Return:
   ```ts
   return {
     content: [{
       type: 'text' as const,
       text: JSON.stringify({
         marker: '[wizard] Context accumulation complete — tool execution lands in Phase 10',
         selectedFindings,
         skippedFindings,
         accumulatedContext: acc,
         contextSummary,
       }, null, 2),
     }],
   };
   ```

**Do NOT:**
- Ask for optional businessContext fields — only `contextRequired` drives the gap-fill (RESEARCH Open Question 1, Anti-pattern 1).
- Wrap individual elicitInput calls in try/catch — rely on the outer handler `catch(err)` (RESEARCH Anti-pattern 6, Phase 8 precedent).
- Mutate `selectedFindings` — it must be passed through unchanged (RESEARCH Anti-pattern 5).
- Change the `[wizard] Issue selection complete` marker anywhere above the replaced `return` — Phase 8 scenarios still reference it via the old path; only the FINAL return block is replaced. Actually, verify: the Phase 8 marker is INSIDE the block being replaced. After replacement, that marker string no longer appears in the file. Update Scenario A's expected marker accordingly — see Task 2.
  </action>
  <verify>
Run `npm run build` — must complete with zero TypeScript errors. Then run `grep -c "TOOL_FIELD_MAP" src/tools/index.ts` — must return at least 3 (declaration + at least two usages). Then run `grep "Context accumulation complete" src/tools/index.ts` — must find exactly one match in the accumulator return. Then run `grep -c "Issue selection complete — fix generation lands in Phase 9" src/tools/index.ts` — must return 0 (that marker was inside the replaced block).
  </verify>
  <done>
`src/tools/index.ts` compiles cleanly with the new types and `TOOL_FIELD_MAP` at module scope. The `if (useWizard)` branch's final return is replaced by the accumulator loop. The loop correctly: seeds from upfront `businessContext`; iterates `selectedFindings`; skips findings without a known tool; skips tools with zero missing required fields; elicits only missing required fields per tool; merges responses; post-processes `services` (string→string[]) and `schemaTypes` (non-array→array); treats gap-fill cancel as per-tool skip; returns the Phase 10 envelope with `marker`, `selectedFindings`, `skippedFindings`, `accumulatedContext`, `contextSummary`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend smoke test with three scenarios covering CTX-01, CTX-02, CTX-03, and update Scenario A's Phase 9 marker</name>
  <files>scripts/smoke-audit-wizard-fork.mjs</files>
  <action>
Edit `scripts/smoke-audit-wizard-fork.mjs`. Keep Scenarios B, C, D, E, F byte-identical.

**Update Scenario A (Phase 9 marker migration):**

Scenario A's current assertion `text.includes('Issue selection complete — fix generation lands in Phase 9')` is now stale — after Phase 9, the wizard path returns the Phase 9 envelope. Update Scenario A to:
- Keep the two-call handler (call 1 = mode fork, call 2 = issue selection accept-all).
- Add gap-fill handling for calls 3+: for any additional elicit call (which will happen because repo audit findings need tool-specific fields like `outputPath`, `robotsPath`, `outputDir`, `schemaTypes`), accept with synthesized values covering every property in `req.params.requestedSchema.properties`. Populate string fields with a dummy like `'/tmp/smoke-placeholder'`, arrays (schemaTypes) with `['LocalBusiness']`. A single generic accept branch handles calls 3 through 7 without per-call branching.
- Update the marker assertion to `'Context accumulation complete — tool execution lands in Phase 10'`.
- Assert `Array.isArray(parsed.selectedFindings)` AND `Array.isArray(parsed.skippedFindings)` AND `typeof parsed.accumulatedContext === 'object'` AND `typeof parsed.contextSummary === 'string'`.

Because the current repo will surface fail findings (e.g., llms-txt fail, robots-ai fail, schema fail, markdown-mirrors fail — `configure_robots_txt` and `generate_llms_txt` are virtually guaranteed), the test needs to survive whatever gap-fills the accumulator triggers. The generic call-3+ handler above ensures that.

**Add Scenario G — CTX-01 upfront context reuse:**

Provide full `businessContext` (businessName, businessType, location, services, website, phoneNumber, description). Assert:
1. Only calls 1 (mode fork) and 2 (issue selection) ask for `mode` or `selectedIssues`.
2. For call 3 and beyond (gap-fills for tool-specific fields like `outputPath`, `robotsPath`), inspect `req.params.requestedSchema.properties` and assert NONE of the keys `businessName`, `businessType`, `location`, `services`, `website`, `phoneNumber`, `description` appear. Accept call 3+ with synthesized tool-field values.
3. Final response contains the Phase 9 marker and `accumulatedContext.businessName === 'Acme Wraps'` (the upfront value survives into the envelope).

The generic third-call handler must track all properties seen across all gap-fill calls and assert the seven businessContext keys never appear.

**Add Scenario H — CTX-02 lazy gather (no upfront context):**

No `businessContext` argument. Stateful handler:
- Call 1: accept mode='wizard'.
- Call 2: accept all selectedIssues.
- Calls 3+: track properties across all requestedSchemas. Accept all with synthesized values (strings='placeholder', arrays=['LocalBusiness']).

After the tool call, inspect that:
1. `businessName` appeared in AT LEAST ONE gap-fill schema across calls 3+ (because at least one of `generate_llms_txt`, `generate_schema_markup`, `generate_faq_content` is a likely finding for this repo — if none fire, the scenario should still pass by noting zero businessName asks means all findings were configure_robots_txt / generate_markdown_mirrors, which don't need businessName). Use a conditional: if any finding with `suggestedToolCall` in `['generate_llms_txt','generate_schema_markup','generate_faq_content']` appeared in selectedFindings, then assert `businessNameAsked === true`; otherwise skip. Determine which findings fired by parsing the final response's `selectedFindings`.
2. `businessName` appeared in EXACTLY ONE gap-fill schema (proof of CTX-03 — not re-asked).
3. The final `accumulatedContext.businessName === 'placeholder'` (the synthesized value carried forward).

**Add Scenario I — CTX-03 carry-forward proof (stricter):**

Same setup as Scenario H (no upfront context). This scenario focuses purely on the "no re-ask" invariant:
- Use a `Set<string>` tracking all property keys seen across all elicit calls after call 2.
- At each call 3+, before accepting, assert `NONE` of the properties in the current `req.params.requestedSchema.properties` appear in the already-seen Set (i.e., every gap-fill schema has a disjoint key space from all prior gap-fills). Push new keys into the Set after the assertion.
- Accept with synthesized values.
- Final response has the Phase 9 marker.

This scenario proves CTX-03 mechanically: no key is asked twice, ever.

**Helper refactor (optional but recommended):** Because Scenarios G, H, I all need a "track-and-accept" third-call handler, factor a helper `synthesizeGapFillResponse(req, seen)` that: records property keys into `seen`, builds a `content` object with placeholder values per property type (string/array/number/boolean). Keep it local to the script.

**Add the new scenarios to `main()` in order G, H, I after F:**

```js
await scenarioG();
await scenarioH();
await scenarioI();
```

Do not reorder existing scenarios.
  </action>
  <verify>
Run `npm run build` (already covered by Task 1 but re-run to confirm no smoke-triggered build break). Then run `node scripts/smoke-audit-wizard-fork.mjs` — must print `SMOKE OK` and exit 0. Then `grep -c "async function scenario" scripts/smoke-audit-wizard-fork.mjs` — must return 9 (A, B, C, D, E, F, G, H, I). Then `grep "Context accumulation complete" scripts/smoke-audit-wizard-fork.mjs` — must match in Scenario A's assertion and at least one other scenario.
  </verify>
  <done>
Smoke script has 9 scenarios. Scenario A now asserts the Phase 9 marker and envelope keys. Scenarios G, H, I prove CTX-01 (upfront reuse), CTX-02 (lazy gather), and CTX-03 (no re-ask) respectively, each via stateful handlers that track all elicitation property keys across calls. Running the smoke script end-to-end returns `SMOKE OK`.
  </done>
</task>

</tasks>

<verification>
Full verification chain:

1. **Build check** — `npm run build` completes with zero TypeScript errors. This proves TOOL_FIELD_MAP typing (keyof BusinessContext constraint) and the accumulator's type narrowing are sound.
2. **Smoke check** — `node scripts/smoke-audit-wizard-fork.mjs` prints `SMOKE OK` and exits 0. This proves:
   - Scenario A: Phase 9 envelope returned with all four keys (selectedFindings, skippedFindings, accumulatedContext, contextSummary).
   - Scenario G (CTX-01): Zero businessContext field keys appear in any gap-fill schema when upfront context is complete.
   - Scenario H (CTX-02): Required fields asked lazily only when a tool needs them.
   - Scenario I (CTX-03): Every gap-fill schema has a disjoint property set — no key ever re-asked.
3. **Source inspection** — `TOOL_FIELD_MAP` present with 5 entries; `Context accumulation complete` marker present exactly once; `Issue selection complete — fix generation lands in Phase 9` marker absent (replaced).
4. **Regression check** — Scenarios B, C, D, E, F still pass (report path, fallback, deselect-all, cancel, all-pass static check). None of their source bytes changed.
</verification>

<success_criteria>
- [ ] `src/tools/index.ts` contains `TOOL_FIELD_MAP`, `AccumulatedContext`, `WizardToolFields` at module scope
- [ ] `if (useWizard)` branch's final return is the Phase 10 envelope (marker, selectedFindings, skippedFindings, accumulatedContext, contextSummary)
- [ ] Accumulator seeds from `businessContext ?? {}` before the loop
- [ ] Per-finding gap-fill elicitation uses only missing required fields (both context and tool-specific)
- [ ] `services` post-processed from string to string[] after merge
- [ ] `schemaTypes` post-processed to string[] after merge
- [ ] User cancel on gap-fill → push to `skippedFindings`, `continue` (not abort wizard)
- [ ] Findings without `suggestedToolCall` or with unknown tool name skipped silently
- [ ] `npm run build` passes
- [ ] `node scripts/smoke-audit-wizard-fork.mjs` prints `SMOKE OK`
- [ ] Scenarios A, G, H, I assert the Phase 9 contract; B, C, D, E, F unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/09-context-accumulation/09-01-SUMMARY.md` per the summary template. Include the Phase 10 input contract verbatim (the JSON shape Phase 10 will consume) under a `## Phase 10 Input Contract` heading — the Phase 10 planner will read this directly.
</output>
